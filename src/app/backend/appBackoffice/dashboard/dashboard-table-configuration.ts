import { DEFAULT_PAGINATION_SIZE } from '../../../core/models/api-response.model';

export const TABLE_CONFIGURATION = {
    paginationSize: DEFAULT_PAGINATION_SIZE,
    displayedColumns: ['name', 'count'],
    columnsName: ['Elemento', 'Cantidad'],
}
